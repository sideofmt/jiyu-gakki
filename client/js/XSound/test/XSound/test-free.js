describe('XSound TEST', function() {

    describe('XSound.free', function() {
        /*
        var C = X.clone();

        X.free([
            C('oscillator'),
            C('oneshot'),
            C('audio'),
            C('media'),
            C('fallback'),
            C('stream'),
            C('mixer'),
            C('mml')
        ]);

        it('should return null', function() {
            expect(C('oscillator')).toBeNull();
            expect(C('oneshot')).toBeNull();
            expect(C('audio')).toBeNull();
            expect(C('media')).toBeNull();
            expect(C('fallback')).toBeNull();
            expect(C('stream')).toBeNull();
            expect(C('mixer')).toBeNull();
            expect(C('mml')).toBeNull();
        });
        */
    });

});
